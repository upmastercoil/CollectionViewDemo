import UIKit

struct Stamp {
    
    var year: Int
    var image: UIImage
    //No need for an initializer, as a struct with no default value for its stored properties, will receive a memeberwise initializer automatically
}
